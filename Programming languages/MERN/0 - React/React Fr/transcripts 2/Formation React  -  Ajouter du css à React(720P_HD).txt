 Bonjour à tous et bienvenue dans ce nouvel épisode de notre relation React dans laquelle nous allons apprendre ensemble à créer des applications web avec le freinbois CREACT. Dans ce nouvel épisode, nous allons voir comment ajouter du code CSS à un composant React. En l'occurrence, j'ai déjà prévu du CSS pour vous. Vous allez sur ce lien que je vous laisse redondre la description. Vous allez les charger. Vous aurez un fichier rare. Je les mets sur mon bureau. Je vais l'extraire et le filier. Je viens de fichier à .css et puis helpers.js. Je vais les déplacer dans mon dossier timer. Que je vais ouvrir. Je les coupe pour le mettre à la racine du dossier source. Comme ceci. Et sur le dessus de code, je mets de fichier qui s'affiche à .css et puis helpers.js. Alors, helpers.js contient juste des fonctions que nous allons utiliser plus tard. Pour ne pas prendre les temps de coder tout ça. Quand on suppose qu'on connaît tout, il y a face script. On va aller un peu plus vite. Et je rappelle qu'on va devoir installer UID. ID en le fragouteur et dans app.css, je te dis cause de css que j'ai préféré pour vous pour toute l'application histoire de gagner du temps. Alors pour faire bref, ujnc des flexbox, donc beaucoup d'endroits, j'espère que vous connaissez. Alors pour revenir à l'application en voyant app.gss, c'est ici qu'on va importer. Quand je vous l'avais dit à la première partie, on import les modules d'entre en a besoin. Alors je fais import, je mets le duplicate, je fais du nom, de fichier, je fais le son extension, qui est css, comme css. Et pour commencer, je donne une classe à cette palise 10, qui sera à et quand je retourne dans box, je vous aussi ajoute des classes. Ici ça sera box, tirer tirer, view. Et ici ça sera box, tirer, view. En d'osko, en d'osko, box. Alors on se regarde et on voit ce que ça donne. Ah désolé je commis le légère erreur, décidément, j'arrête pas de mettre des erreurs aujourd'hui, désolé. On doit plus de mettre le chemin du fichier que l'on veut importer, comme je sais. On va se sauver grader en bas ce que ça donne. Voilà, je le compose en app il est là, je le compose en box, le discontainer, actioncontainer, en a du css, qui est appliqué. Alors vous vous démenterez sûrement pourquoi je mets le css, qui est dans un seul fichier, c'est parce que le fichier, le composant source, qui comprend tous les autres composants. C'est le composant app, donc si je passe ce fichier ici, il sera posant compte aussi pour tous les autres composants qui dépendent du composant app en quelque sorte. Voilà. Alors ça sera tout pour cette vidéo. On se revoit très bientôt pour la suite. Alors décidez la portez-vous bien, likez la vidéo si vous l'avez aimé, partagez l'autor de vous, laissez-moi le commentaire, devrez vous les questions si vous en avez et surtout abonnez-vous à la chaîne si ce n'est pas encore hiçbir Ann probissement de rémuntoie à l'ście.