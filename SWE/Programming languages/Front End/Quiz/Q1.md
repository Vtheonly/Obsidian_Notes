---
sources:
  - "[[1.01 Syntax and Lexical Structure]]"
---

## Related Vault Material

- [[1.01 Syntax and Lexical Structure]] — the chapter this quiz assesses.

This quiz is the retrieval/practice layer for the chapter; use the chapter for learning and the quiz for active recall and verification.

> [!question] JavaScript source code is first processed as a stream of characters and then interpreted as a stream of meaningful tokens before syntactic structure is built.
>> [!success]- Answer
>> True

> [!question] Which stage determines where identifiers, keywords, literals, and punctuators begin and end?
> a) Lexical grammar
> b) Runtime execution
> c) Garbage collection
> d) Type checking
>> [!success]- Answer
>> a) Lexical grammar

> [!question] Match each concept with its primary role.
>> [!example] Group A
>> a) Character stream
>> b) Lexical grammar
>> c) Token stream
>> d) Syntactic grammar
>
>> [!example] Group B
>> n) Determines how tokens can be combined into syntactic structures.
>> o) The raw sequence of source characters.
>> p) The meaningful lexical units produced from source characters.
>> q) Determines how source characters form tokens.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> q)
>> c) -> p)
>> d) -> n)

> [!question] The token sequence can be syntactically invalid even when each individual token is lexically valid.
>> [!success]- Answer
>> True

> [!question] Which statement best distinguishes lexical grammar from syntactic grammar?
> a) Lexical grammar executes code, while syntactic grammar optimizes it.
> b) Lexical grammar determines tokens, while syntactic grammar determines how tokens form structures.
> c) Lexical grammar handles types, while syntactic grammar handles memory.
> d) Lexical grammar runs after execution, while syntactic grammar runs before tokenization.
>> [!success]- Answer
>> b) Lexical grammar determines tokens, while syntactic grammar determines how tokens form structures.

> [!question] Match the example with the kind of reasoning it primarily demonstrates.
>> [!example] Group A
>> a) `leta`
>> b) `const = 42`
>> c) `return\nvalue`
>> d) `/abc/.test(x)`
>
>> [!example] Group B
>> n) Restricted production and LineTerminator behavior
>> o) A lexical unit that remains one identifier
>> p) A syntactically invalid arrangement of otherwise recognizable tokens
>> q) Context-sensitive interpretation involving a regular-expression literal
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)
>> d) -> q)

> [!question] In `return\nvalue`, the newline always means that JavaScript simply treats every newline as a semicolon.
>> [!success]- Answer
>> False

> [!question] What is the most important reason that `return\nvalue` does not behave like `return value`?
> a) The identifier `value` becomes invalid after a newline.
> b) `return` is a restricted production and a LineTerminator prevents the expression from following it in that position.
> c) The JavaScript engine ignores the word `value`.
> d) Newlines are illegal inside functions.
>> [!success]- Answer
>> b) `return` is a restricted production and a LineTerminator prevents the expression from following it in that position.

> [!question] Match each source form with the structural hazard it illustrates.
>> [!example] Group A
>> a) `const x = compute()\n[1, 2].forEach(log)`
>> b) `const x = compute()\n\`template\``
>> c) `const x = foo\n/abc/.test(x)`
>> d) `return\n42`
>
>> [!example] Group B
>> n) A leading template literal may continue the previous expression as a tagged template.
>> o) A leading regular-expression literal may be affected by expression context and token interpretation.
>> p) A restricted production can trigger a statement boundary through ASI.
>> q) A leading bracket can be interpreted as continuation of the previous expression.
>
>> [!success]- Answer
>> a) -> q)
>> b) -> n)
>> c) -> o)
>> d) -> p)

> [!question] Automatic Semicolon Insertion means that JavaScript inserts a semicolon after every newline in the source code.
>> [!success]- Answer
>> False

> [!question] Which situation should immediately trigger an engineer to inspect Automatic Semicolon Insertion rules?
> a) A function uses a loop.
> b) A newline appears in a place where a grammar production might be restricted or a statement might continue.
> c) A variable is declared with `const`.
> d) A program contains a numeric literal.
>> [!success]- Answer
>> b) A newline appears in a place where a grammar production might be restricted or a statement might continue.

> [!question] Match the term with the most accurate description.
>> [!example] Group A
>> a) Automatic Semicolon Insertion
>> b) Restricted production
>> c) LineTerminator
>> d) Statement continuation hazard
>
>> [!example] Group B
>> n) A grammar rule whose interpretation is affected by a line terminator at a specific position.
>> o) A newline-like source character that is significant to certain grammar rules.
>> p) A situation where what looks like a new statement can legally continue the previous expression.
>> q) Grammar-driven insertion of a semicolon when the source omits one and the rules permit insertion.
>
>> [!success]- Answer
>> a) -> q)
>> b) -> n)
>> c) -> o)
>> d) -> p)

> [!question] Which code demonstrates a leading-bracket statement-continuation hazard?
> a) `const x = 1;\nconst y = 2;`
> b) `const x = compute()\n[1, 2].forEach(log)`
> c) `let x = 5;`
> d) `if (x) { y(); }`
>> [!success]- Answer
>> b) `const x = compute()\n[1, 2].forEach(log)`

> [!question] A newline before a leading `[`, `(`, or template literal should always be assumed to start a completely independent statement.
>> [!success]- Answer
>> False

> [!question] Why can `const x = compute()\n[1, 2].forEach(log)` be dangerous?
> a) Arrays cannot appear after function calls.
> b) The leading `[` can be interpreted as part of a continuation of the previous expression instead of necessarily starting an independent statement.
> c) `forEach` is a reserved keyword.
> d) Newlines invalidate array literals.
>> [!success]- Answer
>> b) The leading `[` can be interpreted as part of a continuation of the previous expression instead of necessarily starting an independent statement.

> [!question] Match each leading token with the reason it can deserve special attention at the beginning of a new line.
>> [!example] Group A
>> a) `[`
>> b) `(`
>> c) `\``
>> d) `/`
>
>> [!example] Group B
>> n) Can participate in a tagged template continuation.
>> o) Can begin an expression that may interact with the previous expression's structure.
>> p) Can begin an array literal that may be interpreted as continuation of the previous expression.
>> q) Can require contextual disambiguation between division and a regular-expression literal.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)
>> d) -> q)

> [!question] The slash character `/` has only one syntactic meaning in JavaScript: division.
>> [!success]- Answer
>> False

> [!question] What are the two important interpretations that must be distinguished when JavaScript encounters `/`?
> a) Comment or identifier
> b) Division operator or regular-expression literal
> c) String delimiter or template delimiter
> d) Keyword or number
>> [!success]- Answer
>> b) Division operator or regular-expression literal

> [!question] Match the source form with the interpretation of `/` that it demonstrates.
>> [!example] Group A
>> a) `x / 2`
>> b) `/abc/.test(x)`
>> c) `x / y / z`
>> d) `/foo/g`
>
>> [!example] Group B
>> n) Regular-expression literal
>> o) Division operator
>> p) Division used again while continuing an expression
>> q) Regular-expression literal with a flag
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)
>> d) -> q)

> [!question] The interpretation of `/` can depend on the syntactic context in which it appears.
>> [!success]- Answer
>> True

> [!question] Which statement best explains why JavaScript cannot always decide whether `/` starts a regular-expression literal by looking at that character alone?
> a) The character is randomly interpreted by the engine.
> b) The same character sequence can participate in different grammatical structures depending on what can legally occur at that position.
> c) Regular expressions are processed only at runtime.
> d) Division and regular expressions use different characters internally.
>> [!success]- Answer
>> b) The same character sequence can participate in different grammatical structures depending on what can legally occur at that position.

> [!question] Match the lexical or syntactic category with its role in the source-processing pipeline.
>> [!example] Group A
>> a) Character
>> b) Token
>> c) AST
>> d) Execution
>
>> [!example] Group B
>> n) The runtime stage where program behavior occurs.
>> o) A single source-code unit such as a letter, punctuation mark, or line terminator.
>> p) A meaningful lexical unit such as an identifier, literal, keyword, or punctuator.
>> q) A tree representation of syntactic relationships.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> q)
>> d) -> n)

> [!question] Which sequence best represents the conceptual front-end pipeline discussed in this session?
> a) AST -> characters -> tokens -> execution
> b) Characters -> tokens -> syntactic structure/AST -> execution
> c) Tokens -> characters -> execution -> AST
> d) Execution -> AST -> tokens -> characters
>> [!success]- Answer
>> b) Characters -> tokens -> syntactic structure/AST -> execution

> [!question] The source text's visual formatting is always a reliable representation of the AST the parser constructs.
>> [!success]- Answer
>> False

> [!question] Why is reasoning directly from the visual appearance of JavaScript source code unreliable?
> a) JavaScript ignores all punctuation.
> b) Tokenization, grammar, LineTerminator rules, and contextual interpretation can produce a structure different from the one suggested by indentation or line breaks.
> c) The AST is generated only after execution.
> d) Source code has no relationship to syntax.
>> [!success]- Answer
>> b) Tokenization, grammar, LineTerminator rules, and contextual interpretation can produce a structure different from the one suggested by indentation or line breaks.

> [!question] Match each debugging question with the schema it should trigger.
>> [!example] Group A
>> a) "Why did this newline change the behavior?"
>> b) "Why did these two lines become one expression?"
>> c) "Why is this slash interpreted unexpectedly?"
>> d) "Why does minified code behave differently?"
>
>> [!example] Group B
>> n) Token boundaries, ASI, and statement-continuation hazards
>> o) ASI and LineTerminator-sensitive grammar
>> p) Contextual distinction between division and RegExpLiteral
>> q) Whether the source relied on whitespace or line structure to imply a boundary that the grammar did not require
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)
>> d) -> q)

> [!question] Which of the following is a valid statement about `leta`?
> a) It must be tokenized as the keyword `let` followed by identifier `a`.
> b) It is necessarily a syntax error because `let` is a keyword prefix.
> c) It can be a single identifier rather than being split into `let` and `a`.
> d) It is automatically converted into `let a`.
>> [!success]- Answer
>> c) It can be a single identifier rather than being split into `let` and `a`.

> [!question] A lexically recognizable token sequence is not necessarily a syntactically valid JavaScript program.
>> [!success]- Answer
>> True

> [!question] Which example best demonstrates the distinction between lexical validity and syntactic validity?
> a) `const = 42`
> b) `let x = 42`
> c) `/abc/.test(x)`
> d) `return value`
>> [!success]- Answer
>> a) `const = 42`

> [!question] Match each concept with the incorrect intuition it is meant to replace.
>> [!example] Group A
>> a) ASI
>> b) Lexical grammar
>> c) Syntactic grammar
>> d) Contextual slash interpretation
>
>> [!example] Group B
>> n) "Every newline creates a statement boundary."
>> o) "The source is understood directly as semantic meaning without tokenization."
>> p) "Any sequence of recognizable tokens is automatically valid syntax."
>> q) "The slash character has one fixed meaning everywhere."
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)
>> c) -> p)
>> d) -> q)
