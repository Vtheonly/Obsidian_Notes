# 00 - Generative Models: Chapter Overview

> **Prerequisites:** [[04.1 - Neural Network Basics]], [[08 - Attention and Transformers]] (for the text encoders used in modern generative models), [[02. The Information Bottleneck and Latent Space]] (the conceptual foundation)
> **Connections:** All of these models are Encoder-Decoder architectures (see [[02. The Information Bottleneck and Latent Space]]) specialized for generation rather than classification.

---

## Why this chapter exists

Everything in the vault so far has been **discriminative** — models that predict a label or value from an input. A CNN classifies an image; a Transformer translates a sentence; an MLP regresses a price. These models answer the question: "given input $X$, what is $Y$?"

**Generative** models answer a different question: "given a training set of examples, how can I generate *new* examples that look like they came from the same distribution?" They learn the data distribution $p(X)$ itself, rather than $p(Y|X)$.

This capability underpins modern AI's most visible applications:

- **Image generation:** DALL-E, Stable Diffusion, Midjourney
- **Text generation:** GPT, Claude, LLaMA (covered in Chapter 08 as decoder-only Transformers)
- **Code generation:** Codex, Copilot
- **Audio synthesis:** music generation, voice cloning
- **Drug discovery:** generating novel molecular structures
- **Data augmentation:** generating synthetic training data

This chapter covers the four major families of generative models: **autoencoders** (reconstruction), **variational autoencoders** (probabilistic generation), **GANs** (adversarial generation), and **diffusion models** (iterative denoising). Text generation is covered in Chapter 08 (decoder-only Transformers); this chapter focuses on the *other* modalities, especially images.

---

## Learning path

### 10.1 - Autoencoders
- [[01. The Autoencoder - Compression as Learning]]
- [[02. Denoising Autoencoders and Sparse Autoencoders]]

### 10.2 - Variational Autoencoders
- [[01. From Autoencoder to VAE - Learning a Distribution]]
- [[02. The Reparameterization Trick]]
- [[03. The KL Divergence Loss]]

### 10.3 - GANs
- [[01. The Adversarial Game - Generator vs Discriminator]]
- [[02. Training Dynamics and Mode Collapse]]
- [[03. Wasserstein GANs and Modern Variants]]

### 10.4 - Diffusion Models
- [[01. The Forward Process - Adding Noise]]
- [[02. The Reverse Process - Learning to Denoise]]
- [[03. Score-Based and Latent Diffusion]]

---

## The conceptual arc

The chapter follows a progression: each family fixes a limitation of the previous one.

1. **Autoencoders** can reconstruct their input but cannot generate *new* examples — the latent space is unstructured, so sampling a random latent vector usually produces garbage.

2. **VAEs** fix this by forcing the latent space to be a known distribution (Gaussian). You can sample a random latent vector and decode it into a valid new example. But VAE samples are often blurry — the Gaussian assumption and KL regularization smooth out high-frequency detail.

3. **GANs** fix the blurriness by replacing the reconstruction loss with an adversarial game. A discriminator network tries to distinguish real from generated; the generator tries to fool it. GAN samples are sharp and realistic. But GANs are notoriously hard to train — mode collapse (generating only a few types of examples) and training instability are persistent problems.

4. **Diffusion models** fix the training instability by replacing the adversarial game with a gradual denoising process. The model learns to reverse a noise-adding process, and generation is the iterative removal of noise. Diffusion models are stable to train, produce state-of-the-art quality, and underlie DALL-E 3, Stable Diffusion, and Sora. They are the current state of the art for image generation.

By the end of the chapter, you will understand why each family exists, what it fixes, and what it cannot do — and you will be ready to read modern papers in generative AI.

---

*Connected chapters:* [[08 - Attention and Transformers]] (decoder-only models for text generation, text encoders for conditional generation), [[02. The Information Bottleneck and Latent Space]] (the Encoder-Decoder paradigm these models instantiate).
