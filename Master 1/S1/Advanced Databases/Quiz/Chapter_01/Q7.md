---
sources:
  - "[[01 Views]]"
  - "[[8. 20 simple query]]"
  - "[[6. Practice Exercises]]"
  - "[[5. The Division Operator]]"
---

> [!question] A standard SQL view is a physical table that stores result sets on the disk.
>> [!success]- Answer
>> False

> [!question] The `WITH CHECK OPTION` clause prevents updates or inserts through a view that would violate the view's WHERE condition.
>> [!success]- Answer
>> True

> [!question] You can always perform an `INSERT` operation on a view that contains a `GROUP BY` clause.
>> [!success]- Answer
>> False

> [!question] Using `UNION ALL` is faster than `UNION` because it skips the duplicate removal process.
>> [!success]- Answer
>> True

> [!question] In SQL, `NULL = NULL` evaluates to True.
>> [!success]- Answer
>> False

> [!question] Aggregating functions like `SUM` or `AVG` can be used directly in a `WHERE` clause.
>> [!success]- Answer
>> False

> [!question] A "Relational Division" query typically identifies entities that are associated with every member of a specific set.
>> [!success]- Answer
>> True

> [!question] The `HAVING` clause is used to filter individual rows before they are grouped.
>> [!success]- Answer
>> False

> [!question] Materialized Views (not covered in basic syntax) store data physically, unlike standard views.
>> [!success]- Answer
>> True

> [!question] If you drop a view, the underlying tables are also deleted from the database.
>> [!success]- Answer
>> False

> [!question] Which clause is used to filter groups *after* aggregation?
> a) WHERE
> b) FILTER
> c) HAVING
> d) GROUP
>> [!success]- Answer
>> c) HAVING

> [!question] What is the result of a query comparing a value to NULL using the `=` operator?
> a) True
> b) False
> c) NULL (Unknown)
> d) Error
>> [!success]- Answer
>> c) NULL (Unknown)

> [!question] Which function returns the first non-null value from a list?
> a) IFNULL
> b) COALESCE
> c) ISNULL
> d) NULLIF
>> [!success]- Answer
>> b) COALESCE

> [!question] What is the primary purpose of using a view for security?
> a) To increase database speed
> b) To hide columns from unauthorized users
> c) To create a backup of the data
> d) To automatically sort rows
>> [!success]- Answer
>> b) To hide columns from unauthorized users

> [!question] In the "Chef de Service" exercise, how do you find the maximum of a sum?
> a) `MAX(SUM(x))`
> b) `SUM(MAX(x))`
> c) By creating a view for the `SUM` and querying the `MAX` from that view
> d) Using `GROUP BY ALL`
>> [!success]- Answer
>> c) By creating a view for the `SUM` and querying the `MAX` from that view

> [!question] What does `EXISTS` check in a subquery?
> a) If the subquery returns at least one row
> b) If the subquery returns exactly one row
> c) If the subquery returns no rows
> d) If the subquery is valid syntax
>> [!success]- Answer
>> a) If the subquery returns at least one row

> [!question] When simulating an `INTERSECT` in MySQL, which operator is commonly used?
> a) UNION
> b) IN
> c) JOIN
> d) EXCEPT
>> [!success]- Answer
>> b) IN

> [!question] Which practice is recommended to keep database performance high?
> a) `SELECT *` in all queries
> b) `UNION` instead of `UNION ALL`
> c) Using indices on columns used in `WHERE`
> d) Using functions on indexed columns (e.g., `YEAR(date)`)
>> [!success]- Answer
>> c) Using indices on columns used in `WHERE`

> [!question] What does `UNION` do?
> a) Joins columns from two tables
> b) Combines result sets and removes duplicates
> c) Multiplies every row
> d) Filters rows by a condition
>> [!success]- Answer
>> b) Combines result sets and removes duplicates

> [!question] Why is `SELECT 1 FROM ... LIMIT 1` better than `SELECT COUNT(*)` for existence checks?
> a) It is always slower
> b) It stops searching after finding the first match
> c) It counts all rows in the table
> d) It requires more memory
>> [!success]- Answer
>> b) It stops searching after finding the first match

> [!question] Match the SQL concept with its definition.
>> [!example] Group A
>> a) View
>> b) Materialized View
>> c) Aggregate Function
>> d) View Update Limitation
>
>> [!example] Group B
>> n) Functions like SUM, AVG
>> o) Virtual table (saved query)
>> p) Physically stored result set
>> q) Cannot update views with GROUP BY
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)
>> d) -> q)

> [!question] Match the set/join operator with its outcome.
>> [!example] Group A
>> a) UNION ALL
>> b) INTERSECT
>> c) EXCEPT
>> d) INNER JOIN
>
>> [!example] Group B
>> n) A and B intersection
>> o) A minus B
>> p) Combines A and B with duplicates
>> q) Links rows horizontally
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)
>> d) -> q)

> [!question] Match the SQL clause with its purpose.
>> [!example] Group A
>> a) WHERE
>> b) GROUP BY
>> c) HAVING
>> d) ORDER BY
>
>> [!example] Group B
>> n) Organizes rows into groups
>> o) Sorts final output
>> p) Filters individual rows
>> q) Filters aggregated groups
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> q)
>> d) -> o)

> [!question] Match the subquery operator with its logic.
>> [!example] Group A
>> a) IN
>> b) NOT EXISTS
>> c) ALL
>> d) ANY
>
>> [!example] Group B
>> n) Compares to every result
>> o) Checks if list is empty or matches
>> p) Checks if at least one matches
>> q) Checks existence of rows
>
>> [!success]- Answer
>> a) -> o)
>> b) -> q)
>> c) -> n)
>> d) -> p)

> [!question] Match the "Division" logic concept.
>> [!example] Group A
>> a) Division ($\div$)
>> b) SQL Double NOT EXISTS
>
>> [!example] Group B
>> n) "No course that the student did NOT take"
>> o) "Find candidates associated with EVERY target"
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the performance optimization tip.
>> [!example] Group A
>> a) Avoid `SELECT *`
>> b) Avoid `YEAR(date) = 2023`
>> c) Use `UNION ALL`
>
>> [!example] Group B
>> n) Over duplicates unnecessary sorting
>> o) Prevents index usage
>> p) Transfer only needed data
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the NULL handling method.
>> [!example] Group A
>> a) `IS NULL`
>> b) `COALESCE(x, y)`
>
>> [!example] Group B
>> n) Returns y if x is NULL
>> o) Correct syntax for filtering NULLs
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the View restriction.
>> [!example] Group A
>> a) ORDER BY
>> b) GROUP BY
>
>> [!example] Group B
>> n) Often forbidden in View creation (do it when querying)
>> o) Forbidden for updatable views
>
>> [!success]- Answer
>> a) -> n)
>> b) -> o)

> [!question] Match the logic error.
>> [!example] Group A
>> a) Update with GROUP BY
>> b) Filtering Aggregates
>
>> [!example] Group B
>> n) Cannot use WHERE, must use HAVING
>> o) Database cannot infer which rows to modify
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the view maintenance command.
>> [!example] Group A
>> a) CREATE OR REPLACE
>> b) DROP
>
>> [!example] Group B
>> n) Removes definition entirely
>> o) Updates definition without dropping permissions
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)