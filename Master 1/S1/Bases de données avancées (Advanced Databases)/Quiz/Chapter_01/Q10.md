---
sources:
  - "[[4. Relational Algebra to SQL]]"
  - "[[1. Relational Algebra - The Join Family]]"
  - "[[2. Master Guide to SQL Joins]]"
---

> [!question] The Natural Join ($\bowtie$) operator automatically joins on columns with the same name and retains all duplicate columns in the output.
>> [!success]- Answer
>> False

> [!question] In a Left Outer Join, if no match is found in the Right table, the columns from the Right table are filled with NULL values.
>> [!success]- Answer
>> True

> [!question] An Anti-Join returns rows from the first table that have at least one match in the second table.
>> [!success]- Answer
>> False

> [!question] The Relational Algebra expression $R \div S$ is used to find entities in R associated with every entity in S.
>> [!success]- Answer
>> True

> [!question] The SQL command `SELECT * FROM A, B` performs a Cross Join (Cartesian Product).
>> [!success]- Answer
>> True

> [!question] In Relational Algebra, the selection operator ($\sigma$) is used to filter vertical columns.
>> [!success]- Answer
>> False

> [!question] The `UNION` operation in SQL automatically removes duplicate rows from the final result set.
>> [!success]- Answer
>> True

> [!question] A Theta-Join ($\bowtie_\theta$) is defined as a Cartesian Product followed by a Selection.
>> [!success]- Answer
>> True

> [!question] Using `SELECT *` is considered a best practice for large-scale production databases to ensure data integrity.
>> [!success]- Answer
>> False

> [!question] MySQL natively supports the `FULL OUTER JOIN` keyword.
>> [!success]- Answer
>> False

> [!question] Which Relational Algebra operator is represented by the symbol $\pi$?
> a) Selection
> b) Projection
> c) Join
> d) Union
>> [!success]- Answer
>> b) Projection

> [!question] Which type of join returns only rows where a match is found in both tables?
> a) Left Join
> b) Full Join
> c) Inner Join
> d) Cross Join
>> [!success]- Answer
>> c) Inner Join

> [!question] How can you simulate an `INTERSECT` in MySQL?
> a) USING `UNION`
> b) USING `WHERE IN`
> c) USING `CROSS JOIN`
> d) USING `LEFT JOIN`
>> [!success]- Answer
>> b) USING `WHERE IN`

> [!question] What is the mathematical size of a Cartesian Product $R \times S$ if $|R|=10$ and $|S|=5$?
> a) 2
> b) 15
> c) 50
> d) 100
>> [!success]- Answer
>> c) 50

> [!question] Which operator is used for the "Division" operation in Relational Algebra?
> a) $\bowtie$
> b) $\sigma$
> c) $\div$
> d) $\pi$
>> [!success]- Answer
>> c) $\div$

> [!question] Which of the following is true about a "Self-Join"?
> a) It joins two completely different tables
> b) It joins a table to itself using aliases
> c) It is only used for numerical values
> d) It automatically removes NULLs
>> [!success]- Answer
>> b) It joins a table to itself using aliases

> [!question] What does the wildcard character `_` represent in a `LIKE` pattern?
> a) Zero or more characters
> b) Exactly one character
> c) Exactly two characters
> d) A numeric digit
>> [!success]- Answer
>> b) Exactly one character

> [!question] Which of the following is an Aggregate function?
> a) SUM()
> b) SELECT
> c) JOIN
> d) UNION
>> [!success]- Answer
>> a) SUM()

> [!question] When joining tables, why are Aliases (e.g., `FROM Students S`) recommended?
> a) To increase database storage
> b) To distinguish between tables with the same column names
> c) To force the use of an Inner Join
> d) To automatically delete duplicates
>> [!success]- Answer
>> b) To distinguish between tables with the same column names

> [!question] Which join type is best for finding records in Table A that have no related record in Table B?
> a) Inner Join
> b) Left Join with NULL check
> c) Full Join
> d) Cross Join
>> [!success]- Answer
>> b) Left Join with NULL check

> [!question] Match the Relational Algebra symbol with its SQL equivalent.
>> [!example] Group A
>> a) $\pi$
>> b) $\sigma$
>> c) $\bowtie$
>> d) $-$
>
>> [!example] Group B
>> n) WHERE
>> o) SELECT
>> p) EXCEPT / MINUS
>> q) JOIN
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> q)
>> d) -> p)

> [!question] Match the Join type with its result set.
>> [!example] Group A
>> a) Inner Join
>> b) Left Join
>> c) Cross Join
>> d) Full Join
>
>> [!example] Group B
>> n) All from Left + Matches from Right
>> o) Matches from both only
>> p) All from both tables
>> q) All combinations of A and B
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> q)
>> d) -> p)

> [!question] Match the SQL set operator with its logic.
>> [!example] Group A
>> a) UNION
>> b) INTERSECT
>> c) EXCEPT
>
>> [!example] Group B
>> n) In both A AND B
>> o) In A but NOT in B
>> p) In A OR B
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the Join-related concept with its definition.
>> [!example] Group A
>> a) Theta Join
>> b) Natural Join
>> c) Semi-Join
>> d) Anti-Join
>
>> [!example] Group B
>> n) Joins on identical names, removes duplicates
>> o) Uses arbitrary comparison condition
>> p) Returns rows from A NOT in B
>> q) Returns rows from A that have a match in B
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> q)
>> d) -> p)

> [!question] Match the SQL wildcard/operator with its usage.
>> [!example] Group A
>> a) LIKE '%'
>> b) LIKE '_'
>> c) IS NULL
>> d) IN
>
>> [!example] Group B
>> n) Checking for empty/unknown fields
>> o) Zero or more characters
>> p) Checking against a provided list
>> q) Exactly one character
>
>> [!success]- Answer
>> a) -> o)
>> b) -> q)
>> c) -> n)
>> d) -> p)

> [!question] Match the cardinality/degree terminology.
>> [!example] Group A
>> a) Cardinality
>> b) Arity
>
>> [!example] Group B
>> n) Number of Columns
>> o) Number of Rows
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the logical operator with its SQL use case.
>> [!example] Group A
>> a) ANY
>> b) ALL
>> c) EXISTS
>
>> [!example] Group B
>> n) Returns true if a subquery produces rows
>> o) Compares value to every result in a list
>> p) Compares value to at least one result in a list
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the Join result type with the scenario.
>> [!example] Group A
>> a) Match found
>> b) No match (Outer)
>
>> [!example] Group B
>> n) NULL values added
>> o) Rows combined normally
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Relational Algebra set requirement.
>> [!example] Group A
>> a) Same Arity
>> b) Domain Compatibility
>
>> [!example] Group B
>> n) Columns have same data type
>> o) Same number of columns
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the Alias usage with its necessity.
>> [!example] Group A
>> a) Simple Select
>> b) Self-Join
>
>> [!example] Group B
>> n) Mandatory to distinguish table instances
>> o) Optional, used for readability
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)