# 1. Basic Nested Loops Complexity

When analyzing algorithms from pseudo-code, the complexity is largely driven by loops. Iterating `n` times yields $O(n)$. Nesting loops multiplies the complexity.

## TD2 Ex1: Algorithm Analysis

### Algorithm Example 1
