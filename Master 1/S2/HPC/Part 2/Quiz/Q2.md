---
sources:
---
---
sources:
  - "[[High Performance Computing]]"
---

> [!question] Modern computers containing 4 or more CPU cores can execute instructions independently with shared access to main memory.
>> [!success]- Answer
>> True

> [!question] In serial processing, if one image takes 10 seconds to process, image 2 can start processing while image 1 is still being processed.
>> [!success]- Answer
>> False

> [!question] Converting serial code to parallel code guarantees a faster execution time, even for very small computational problems.
>> [!success]- Answer
>> False

> [!question] A major drawback of the Shared Memory model is that adding more cores can cause memory contention when accessing the same resource.
>> [!success]- Answer
>> True

> [!question] In an ideal weak scaling scenario, the time to solution increases linearly as the number of cores increases.
>> [!success]- Answer
>> False

> [!question] If a hard disk cannot feed images fast enough to the processors, resulting in idle cores, this is considered an I/O bottleneck.
>> [!success]- Answer
>> True

> [!question] An image filtering loop that requires the result of the previous pixel's calculation can be easily parallelized across multiple cores.
>> [!success]- Answer
>> False

> [!question] A fundamental rule of performance profiling is to never attempt to optimize an algorithm before performing an empirical profile.
>> [!success]- Answer
>> True

> [!question] In Python, the `Profile` module is a C-extension providing minimal overhead, while `cProfile` is implemented in pure Python.
>> [!success]- Answer
>> False

> [!question] Profilers are typically invoked via the command line to avoid modifying the production source code.
>> [!success]- Answer
>> True

> [!question] What are the two primary limitations of a single core that motivate parallelization?
> a) Power consumption and heat generation
> b) Speed limitation and memory requirements
> c) Compiler overhead and storage capacity
> d) Code readability and debugging difficulty
>> [!success]- Answer
>> b) Speed limitation and memory requirements

> [!question] According to the text, what is often the primary source of parallelizable code in an application?
> a) Variable declarations
> b) Import statements
> c) Loops
> d) Print functions
>> [!success]- Answer
>> c) Loops

> [!question] Which characteristic best describes the Shared Memory model?
> a) Processors share data explicitly through network communication
> b) It scales perfectly without any memory contention
> c) Any memory changes are visible to all processors immediately
> d) Each core operates entirely on its own private memory pool
>> [!success]- Answer
>> c) Any memory changes are visible to all processors immediately

> [!question] Dividing the vector calculation of wind speed across a geographic area among different processors is an example of what concept?
> a) Serial Processing
> b) Pipelining
> c) Task Parallelism
> d) Single Instruction, Multiple Data (SIMD)
>> [!success]- Answer
>> c) Task Parallelism

> [!question] If only 50% of a code is parallelizable, what is the maximum theoretical speedup regardless of how many processors are added?
> a) 1.5
> b) 2
> c) 50
> d) Infinite
>> [!success]- Answer
>> b) 2

> [!question] Passing results one way through a sequence of operations (e.g., Generate Image -> Color Image -> Resize Image) is an example of which parallel strategy?
> a) Strong Scaling
> b) Vectorization
> c) Load Imbalance
> d) Pipelining
>> [!success]- Answer
>> d) Pipelining

> [!question] High-performance compilers like GCC can utilize profiling data to optimize branch prediction and instruction scheduling. Which compiler flag enables this?
> a) -O3
> b) -fprofile-use
> c) -fopenmp
> d) -lmpi
>> [!success]- Answer
>> b) -fprofile-use

> [!question] In the command `python -m cProfile -s cumtime myscript.py`, what is the purpose of the `-s` argument?
> a) It defines the sort criteria for the diagnostic report
> b) It directs the output to a specific external file
> c) It sets the script to run sequentially
> d) It suppresses all profiling warnings
>> [!success]- Answer
>> a) It defines the sort criteria for the diagnostic report

> [!question] What is the impact of a recursive data dependency in a loop (e.g., `a[i] = a[i-1] + b[i]`)?
> a) The compiler automatically vectorizes the code
> b) Processors are forced to remain idle waiting for a single master thread to finish the computation
> c) It immediately achieves ideal weak scaling
> d) The workload is distributed symmetrically across all nodes
>> [!success]- Answer
>> b) Processors are forced to remain idle waiting for a single master thread to finish the computation

> [!question] In a tabular diagnostic output from a Python profiler, what does the `percall` metric represent?
> a) The total number of times the function was invoked
> b) Calculated as `tottime / ncalls` or `cumtime / ncalls`
> c) The precise line number of the code segment
> d) The total duration spent only in sub-functions
>> [!success]- Answer
>> b) Calculated as `tottime / ncalls` or `cumtime / ncalls`

> [!question] Match the computer execution model with its behavior.
>> [!example] Group A
>> a) Traditional computers
>> b) Modern computers
>
>> [!example] Group B
>> n) Contain 4 or more CPU cores to enable parallel instruction execution.
>> o) Single processing unit executes instructions sequentially.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the memory model to its specific drawback.
>> [!example] Group A
>> a) Shared Memory
>> b) Distributed Memory
>> c) Hybrid Memory
>
>> [!example] Group B
>> n) Complex to implement because the programmer must use both OpenMP and MPI.
>> o) Requires explicit communication programming (e.g., using MPI).
>> p) Adding more cores causes memory contention, which limits scalability.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the parallel computing concept to its analogy used in the presentation.
>> [!example] Group A
>> a) The Relay Race
>> b) Weak Scaling
>> c) Shared Memory
>
>> [!example] Group B
>> n) 1000 satellite images are stored in a single dataset and multiple workers access this exact same space.
>> o) Code is only as fast as its slowest piece (the bottleneck).
>> p) Monitoring live security cameras (1 core processes 1 stream; 100 cores process 100 streams simultaneously).
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the type of systemic bottleneck to its description.
>> [!example] Group A
>> a) Resource Serialization
>> b) Memory Bandwidth
>> c) I/O Systems
>
>> [!example] Group B
>> n) Processes are queuing to read high-resolution frames from a non-parallel file system.
>> o) Parallel processes compete for finite hardware resources, forcing sequential access.
>> p) Multiple cores have saturated the path to RAM.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the source of parallel overhead with its definition.
>> [!example] Group A
>> a) Library/compiler overhead
>> b) Termination time
>> c) Inter-Process Communication (IPC)
>
>> [!example] Group B
>> n) Shutting down parallel processes.
>> o) Distributed programs exchanging data across a network.
>> p) Runtime management costs.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the data parallelism term with its meaning.
>> [!example] Group A
>> a) Vector operation
>> b) Data parallelism
>> c) Task parallelism
>
>> [!example] Group B
>> n) Distributing different compute tasks across processors.
>> o) Distributing parts of a dataset across processors.
>> p) Applying addition, subtraction, multiplication, or division simultaneously.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the Python profiler module to its characteristic.
>> [!example] Group A
>> a) Profile
>> b) cProfile
>
>> [!example] Group B
>> n) A C-extension providing minimal overhead, suitable for long-running high-performance simulations.
>> o) Implemented in pure Python; ideal for extending or modifying the profiler's internal logic.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the performance barrier with its consequence.
>> [!example] Group A
>> a) Hardware Contention: Memory
>> b) Hardware Contention: I/O
>> c) Asymmetric Work Distribution
>
>> [!example] Group B
>> n) Saturated path to RAM slows down execution.
>> o) Causes load imbalance, leading to processor idling at synchronization points.
>> p) Reading from a non-parallel file system causes queuing.
>
>> [!success]- Answer
>> a) -> n)
>> b) -> p)
>> c) -> o)

> [!question] Match the scaling or efficiency metric to its ideal outcome.
>> [!example] Group A
>> a) Ideal Strong Scaling
>> b) Ideal Weak Scaling
>> c) Perfect Scaling Efficiency
>
>> [!example] Group B
>> n) The efficiency value evaluates to exactly 1.0.
>> o) Time to solution decreases as 1/N.
>> p) Time to solution is constant and independent of the core count.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the element of the Pareto Principle ("Vital Few") strategy with its description.
>> [!example] Group A
>> a) General Principle
>> b) Target
>> c) Efficiency Goal
>
>> [!example] Group B
>> n) Identify the 20% of functions causing the primary bottlenecks.
>> o) Focus development effort where the Return on Investment (ROI) is highest.
>> p) Roughly 80% of consequences originate from 20% of causes.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)