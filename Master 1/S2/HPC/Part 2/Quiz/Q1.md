
---
sources:
  - "[[High Performance Computing]]"
---

> [!question] Traditional computers execute instructions sequentially on a single processing unit.
>> [!success]- Answer
>> True

> [!question] Adding more cores to a Shared Memory model always improves performance without any drawbacks or contention.
>> [!success]- Answer
>> False

> [!question] In the Distributed Memory model, changes made to local memory immediately affect other processors.
>> [!success]- Answer
>> False

> [!question] SIMD stands for Single Instruction, Multiple Data.
>> [!success]- Answer
>> True

> [!question] Strong scaling involves increasing the problem size in proportion to the number of cores used.
>> [!success]- Answer
>> False

> [!question] According to the Pareto Principle in HPC, approximately 80% of execution time is concentrated in only 20% of the code.
>> [!success]- Answer
>> True

> [!question] The `cumtime` metric in Python profiling measures only the time spent strictly within a function, excluding sub-functions.
>> [!success]- Answer
>> False

> [!question] Parallel overhead can make parallel execution slower than serial execution for very small problems.
>> [!success]- Answer
>> True

> [!question] Task parallelism involves executing different tasks or instructions on the same dataset across multiple processors.
>> [!success]- Answer
>> True

> [!question] According to fundamental profiling rules, developers should always attempt to optimize an algorithm before performing an empirical profile.
>> [!success]- Answer
>> False

> [!question] Which parallel programming standard is typically used for distributed-memory parallelism across multiple nodes?
> a) OpenMP
> b) MPI
> c) cProfile
> d) SIMD
>> [!success]- Answer
>> b) MPI

> [!question] What does speedup ($S$) measure in parallel computing?
> a) The ratio of the serial execution time to the parallel execution time
> b) The fraction of time for which a processor is usefully utilized
> c) The amount of memory shared between nodes
> d) The initialization time of a parallel environment
>> [!success]- Answer
>> a) The ratio of the serial execution time to the parallel execution time

> [!question] Which of the following is considered a primary source of parallel overhead?
> a) Vectorization
> b) Strong scaling
> c) Inter-process communication
> d) Decreasing time to solution
>> [!success]- Answer
>> c) Inter-process communication

> [!question] What is a key benefit of the Shared Memory model?
> a) No memory contention
> b) Excellent scalability for unlimited expansion
> c) Fast data sharing between cores
> d) Processes share data explicitly through network communication
>> [!success]- Answer
>> c) Fast data sharing between cores

> [!question] In Python profiling using `cProfile`, what does the `tottime` column represent?
> a) Total duration spent in a function and all of its sub-functions
> b) Number of times the function was invoked
> c) Total duration spent strictly within the function, excluding sub-functions
> d) The average execution time per call
>> [!success]- Answer
>> c) Total duration spent strictly within the function, excluding sub-functions

> [!question] Which scalability type keeps the total problem size fixed while increasing the number of processors?
> a) Weak Scaling
> b) Perfect Scaling
> c) Strong Scaling
> d) Linear Scaling
>> [!success]- Answer
>> c) Strong Scaling

> [!question] What happens in a "Load Imbalance" scenario during parallel execution?
> a) One processor reaches synchronization points earlier than others, leading to processor idling
> b) Multiple cores saturate the path to RAM simultaneously
> c) A script executes sequentially on a single core
> d) The initialization overhead dominates the runtime
>> [!success]- Answer
>> a) One processor reaches synchronization points earlier than others, leading to processor idling

> [!question] Which type of data parallelism executes the same logic/program across different segments of a dataset simultaneously?
> a) SPMD
> b) Pipelining
> c) Task Parallelism
> d) Serial Processing
>> [!success]- Answer
>> a) SPMD

> [!question] What is a major drawback of using the Hybrid Memory model?
> a) It has the worst scalability of all memory models
> b) Data sharing within a node is exceptionally slow
> c) It is complex because the programmer must use both OpenMP and MPI
> d) It completely eliminates the ability to use hardware optimizations
>> [!success]- Answer
>> c) It is complex because the programmer must use both OpenMP and MPI

> [!question] According to the "Rule of Thumb" for efficiency in HPC, what efficiency percentage should you aim for?
> a) > 20%
> b) > 50%
> c) > 80%
> d) Exactly 100%
>> [!success]- Answer
>> c) > 80%

> [!question] Match the memory model with its primary characteristic.
>> [!example] Group A
>> a) Shared Memory
>> b) Distributed Memory
>> c) Hybrid Memory
>
>> [!example] Group B
>> n) Combines shared and distributed memory architectures.
>> o) All cores share the same pool of memory.
>> p) Each core has its own private memory.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the performance metric with its corresponding formula.
>> [!example] Group A
>> a) Speedup ($S$)
>> b) Efficiency ($E$)
>> c) Ideal Linear Speedup
>
>> [!example] Group B
>> n) $S = N$
>> o) $S / N$
>> p) $T_s / T_p$
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the scaling concept with its definition.
>> [!example] Group A
>> a) Strong Scaling
>> b) Weak Scaling
>> c) Efficiency
>
>> [!example] Group B
>> n) Problem size increases in proportion to the number of cores.
>> o) Total problem size is fixed while processors increase.
>> p) Characterized as Expected Time divided by Actual/Measured Time.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the parallel strategy with its description.
>> [!example] Group A
>> a) Data Parallelism
>> b) Task Parallelism
>> c) Pipelining
>
>> [!example] Group B
>> n) Passing results one way through a sequence of operations.
>> o) Distributing different compute tasks across processors.
>> p) Distributing different parts of a dataset across processors.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the diagnostic output column from Python profiling with its meaning.
>> [!example] Group A
>> a) `ncalls`
>> b) `tottime`
>> c) `cumtime`
>
>> [!example] Group B
>> n) Total duration spent in a function and all sub-functions it called.
>> o) Number of times a function was invoked.
>> p) Total duration spent strictly within a function.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the source of parallel overhead with its description.
>> [!example] Group A
>> a) Startup time
>> b) Synchronizations
>> c) Communication
>
>> [!example] Group B
>> n) Waiting for all processes to reach a barrier.
>> o) Exchanging data between processes, especially in MPI.
>> p) Initializing the parallel environment.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the parallel tool or standard with its primary use case.
>> [!example] Group A
>> a) OpenMP
>> b) MPI
>> c) cProfile
>
>> [!example] Group B
>> n) C-extension providing minimal overhead for performance profiling.
>> o) Enabling distributed-memory parallelism and network communication.
>> p) Managing shared-memory parallelism within a single node.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the bottleneck type with its specific example.
>> [!example] Group A
>> a) I/O
>> b) Networking
>> c) Load Imbalance
>
>> [!example] Group B
>> n) One GPU receives a complex frame with many objects while others get empty frames.
>> o) MPI communication between distributed nodes takes excessive time.
>> p) The disk cannot feed images fast enough, causing cores to sit idle.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the processing concept with its behavior.
>> [!example] Group A
>> a) SIMD
>> b) SPMD
>> c) Scalar Operation
>
>> [!example] Group B
>> n) Executing the same logic/program across different segments of a dataset.
>> o) Performing mathematical operations sequentially, one at a time.
>> p) Carrying out the same instruction simultaneously multiple times across different dataset elements.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the parallelization rule or principle with its definition.
>> [!example] Group A
>> a) The Pareto Principle
>> b) Loop Rule
>> c) Independence
>
>> [!example] Group B
>> n) Iterations must not depend on previous results.
>> o) Code must be executable independently of other instructions.
>> p) 80% of execution time is concentrated in only 20% of the code.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)
