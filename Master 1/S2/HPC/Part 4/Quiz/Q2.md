---
sources:
  - "[[High Performance Computing Course Slides]]"
---

> [!question] The "Message Passing Interface" (MPI) is a proprietary tool designed exclusively for the Python programming language.
>> [!success]- Answer
>> False

> [!question] In a distributed memory model, each process has its own private address space.
>> [!success]- Answer
>> True

> [!question] When using `mpi4py`, the uppercase `Send`/`Recv` methods provide direct C-style memory access for NumPy buffers.
>> [!success]- Answer
>> True

> [!question] "Embarrassingly parallel" loops are best suited for specialized, non-standard library implementations.
>> [!success]- Answer
>> False

> [!question] Using `MPI.ANY_TAG` allows a process to differentiate between "Data" and "Termination" messages based on their identification tags.
>> [!success]- Answer
>> True

> [!question] The `Scan()` operation in MPI performs a cumulative prefix sum across ranks.
>> [!success]- Answer
>> True

> [!question] Collective operations are non-blocking, meaning they never cause load imbalance.
>> [!success]- Answer
>> False

> [!question] The `Comm_split()` function creates a new communicator based on a `color` parameter.
>> [!success]- Answer
>> True

> [!question] A "Ghost Region" update is a technique used in grid-based simulations to exchange data with neighbor processes.
>> [!success]- Answer
>> True

> [!question] The `Alltoall()` operation is essentially a data transpose where every rank exchanges chunks with every other rank.
>> [!success]- Answer
>> True

> [!question] What does the `root` parameter in `comm.Bcast()` specify?
>> [!question] Which of these is NOT a mentioned benefit of the "Uppercase" `mpi4py` methods?
> a) The rank of the process sending the data
> b) The total number of processes
> c) The buffer size
> d) The communicator's ID
>> [!success]- Answer
>> a) The rank of the process sending the data

> [!question] Which of these is NOT a mentioned benefit of the "Uppercase" `mpi4py` methods?
> a) Near-native speed
> b) Communication of any Python object
> c) Direct C-style memory access
> d) Communication of NumPy buffers
>> [!success]- Answer
>> b) Communication of any Python object

> [!question] What is the primary purpose of `np.split()` in the context of Domain Decomposition?
> a) To increase network latency
> b) To ensure each rank receives a fair share of the workload
> c) To create a deadlock
> d) To initialize a communicator
>> [!success]- Answer
>> b) To ensure each rank receives a fair share of the workload

> [!question] Which MPI function returns a `Request` object instead of blocking?
> a) `Send()`
> b) `Isend()`
> c) `Reduce()`
> d) `Barrier()`
>> [!success]- Answer
>> b) `Isend()`

> [!question] In the "Master-Worker" pattern, how does the master notify workers of the work to be done?
> a) Via shared memory
> b) Via explicit message passing
> c) Automatically
> d) Through global variables
>> [!success]- Answer
>> b) Via explicit message passing

> [!question] What is the result of a `Barrier()` call?
> a) Data is automatically sorted
> b) All processes must reach the barrier before any can proceed
> c) The program terminates immediately
> d) Communication is disabled
>> [!success]- Answer
>> b) All processes must reach the barrier before any can proceed

> [!question] Which of the following is a symptom of a deadlock?
> a) The program runs extremely fast
> b) The program hangs (system is stuck)
> c) The program uses too much memory
> d) The output is out of order
>> [!success]- Answer
>> b) The program hangs (system is stuck)

> [!question] Which parameter is crucial for keeping Monte Carlo simulations consistent across multiple ranks?
> a) Memory size
> b) Network speed
> c) Deterministic seeds (e.g., `random.seed(42 + rank)`)
> d) Number of cores
>> [!success]- Answer
>> c) Deterministic seeds (e.g., `random.seed(42 + rank)`)

> [!question] Why is "Hybrid Parallelism" (e.g., MPI + OpenMP) considered a gold standard?
> a) It is cheaper
> b) MPI handles inter-node, OpenMP handles intra-node threading
> c) It eliminates the need for any programming
> d) It only requires one library
>> [!success]- Answer
>> b) MPI handles inter-node, OpenMP handles intra-node threading

> [!question] What is the main drawback of sending 1000 small messages instead of one large one?
> a) Higher memory usage
> b) Increased network latency impact
> c) Lower precision
> d) Incompatibility with NumPy
>> [!success]- Answer
>> b) Increased network latency impact

> [!question] Match the MPI term with its definition.
>> [!example] Group A
>> a) MPI.COMM_WORLD
>> b) rank
>> c) size
>
>> [!example] Group B
>> n) The total number of processes.
>> o) The default group of all processes.
>> p) A unique identifier for a process.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the MPI collective with its specific communication pattern.
>> [!example] Group A
>> a) Bcast
>> b) Reduce
>> c) Scan
>
>> [!example] Group B
>> n) Cumulative prefix operations across ranks.
>> o) One-to-all data sharing.
>> p) Combining all values into one result on the root.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the optimization strategy with its intended result.
>> [!example] Group A
>> a) Batching
>> b) Non-blocking
>> c) Domain Decomposition
>
>> [!example] Group B
>> n) Hiding communication latency behind computation.
>> o) Reducing the number of network interactions.
>> p) Dividing N items among P processes.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the MPI error or state with its cause.
>> [!example] Group A
>> a) Deadlock
>> b) Type Mismatch
>> c) Load Imbalance
>
>> [!example] Group B
>> n) Sending INT where DOUBLE is expected.
>> o) Waiting for a matching receive that never starts.
>> p) One rank is significantly slower than others at a collective call.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the memory concept with its description.
>> [!example] Group A
>> a) Global Array
>> b) Local Slice
>> c) Memory Window
>
>> [!example] Group B
>> n) A conceptual idea in distributed memory.
>> o) Memory area accessible via RMA (One-Sided Communication).
>> p) The portion of an array an individual rank operates on.
>
>> [!success]- Answer
>> a) -> n)
>> b) -> p)
>> c) -> o)

> [!question] Match the distributed system pattern with its description.
>> [!example] Group A
>> a) SPMD
>> b) Master-Worker
>> c) Hybrid Parallelism
>
>> [!example] Group B
>> n) Mixing MPI with other models like OpenMP or CUDA.
>> o) Using conditional execution to assign different roles.
>> p) Single Program, Multiple Data execution.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the performance metric/law with its definition.
>> [!example] Group A
>> a) Amdahl's Law
>> b) Strong Scaling
>> c) Weak Scaling
>
>> [!example] Group B
>> n) Scaling by fixing work per processor as problem grows.
>> o) Speedup is limited by the serial part of the code.
>> p) Scaling by keeping the total problem size fixed.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the MPI utility with its function.
>> [!example] Group A
>> a) `MPI.Wtime()`
>> b) `status.Get_source()`
>> c) `status.Get_tag()`
>
>> [!example] Group B
>> n) Retrieves the tag of the message received.
>> o) Used for performance profiling of loops.
>> p) Identifies which rank sent the message.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the distribution method with its performance benefit.
>> [!example] Group A
>> a) Block Distribution
>> b) Cyclic Distribution
>> c) Halo Exchange
>
>> [!example] Group B
>> n) Ideal for spatial locality/cache efficiency.
>> o) Exchanging boundary data in grid simulations.
>> p) Good for load balancing when work per index varies.
>
>> [!success]- Answer
>> a) -> n)
>> b) -> p)
>> c) -> o)

> [!question] Match the RMA (One-Sided) concept with its action.
>> [!example] Group A
>> a) MPI.Put()
>> b) MPI.Get()
>> c) Memory Window
>
>> [!example] Group B
>> n) Retrieves data from another process's memory.
>> o) Defines the area of memory available for access.
>> p) Writes data into another process's memory.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)