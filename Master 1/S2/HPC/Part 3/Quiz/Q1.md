---
sources:
  - "[[High Performance Computing - OpenMP]]"
---

> [!question] OpenMP is a library that can be used for both shared-memory and distributed-memory parallel programming.
>> [!success]- Answer
>> False

> [!question] If the OpenMP flag is missing during compilation, the compiler ignores the pragmas and produces a serial program.
>> [!success]- Answer
>> True

> [!question] A "Critical Region" in OpenMP ensures that multiple threads can execute the block of code simultaneously for better performance.
>> [!success]- Answer
>> False

> [!question] In the fork-join model, threads synchronize and terminate at the join point, leaving only the master thread.
>> [!success]- Answer
>> True

> [!question] The `#pragma omp atomic` directive is generally slower than `#pragma omp critical`.
>> [!success]- Answer
>> False

> [!question] The `schedule(static, chunk)` clause performs better than `schedule(dynamic, chunk)` if the work per iteration is highly unpredictable.
>> [!success]- Answer
>> False

> [!question] Every variable used inside an OpenMP parallel region must have a data-sharing attribute.
>> [!success]- Answer
>> True

> [!question] Nested parallelism is enabled by default in all OpenMP implementations to maximize performance.
>> [!success]- Answer
>> False

> [!question] "False Sharing" occurs when multiple threads modify different variables that reside on the same cache line.
>> [!success]- Answer
>> True

> [!question] A thread cannot proceed past an implicit barrier until every thread in the team has reached it.
>> [!success]- Answer
>> True

> [!question] Which environment variable is used to set the number of threads for an OpenMP program?
> a) OMP_THREADS_COUNT
> b) OMP_NUM_THREADS
> c) OMP_THREAD_LIMIT
> d) OMP_PARALLEL_THREADS
>> [!success]- Answer
>> b) OMP_NUM_THREADS

> [!question] Which clause allows a private variable to inherit the value it had before the parallel region?
> a) private()
> b) shared()
> c) firstprivate()
> d) lastprivate()
>> [!success]- Answer
>> c) firstprivate()

> [!question] What is the primary purpose of the `collapse(N)` clause?
> a) To reduce the number of threads.
> b) To merge N nested loops into a single iteration space.
> c) To force all threads to synchronize.
> d) To delete private data.
>> [!success]- Answer
>> b) To merge N nested loops into a single iteration space.

> [!question] Which of these is NOT a valid operator for the `reduction` clause?
> a) +
> b) *
> c) min
> d) /
>> [!success]- Answer
>> d) /

> [!question] What does `omp_get_thread_num()` return?
> a) The total number of threads in the team.
> b) The ID of the calling thread.
> c) The current nesting level.
> d) The maximum number of threads allowed.
>> [!success]- Answer
>> b) The ID of the calling thread.

> [!question] Which directive is used to make a code block execute only on the master thread?
> a) `#pragma omp master`
> b) `#pragma omp single`
> c) `#pragma omp critical`
> d) `#pragma omp atomic`
>> [!success]- Answer
>> a) `#pragma omp master`

> [!question] What is the main characteristic of a "Task" in OpenMP?
> a) It always runs immediately on the current thread.
> b) It can be executed by any available thread in the team.
> c) It is always assigned to the master thread.
> d) It cannot have private data.
>> [!success]- Answer
>> b) It can be executed by any available thread in the team.

> [!question] Which tool is suggested for detecting race conditions at compile-time or runtime?
> a) ThreadSanitizer (TSan)
> b) Linker Optimizer
> c) Cache Profiler
> d) Hardware Monitor
>> [!success]- Answer
>> a) ThreadSanitizer (TSan)

> [!question] What happens when a thread reaches a `#pragma omp barrier`?
> a) It kills the other threads.
> b) It continues execution immediately.
> c) It waits until all other threads in the team reach the barrier.
> d) It restarts the loop.
>> [!success]- Answer
>> c) It waits until all other threads in the team reach the barrier.

> [!question] Why is `default(none)` considered a best practice in OpenMP?
> a) It makes the code faster.
> b) It forces explicit declaration of variable scope to prevent race conditions.
> c) It automatically parallelizes all code.
> d) It removes the need for pragmas.
>> [!success]- Answer
>> b) It forces explicit declaration of variable scope to prevent race conditions.

> [!question] Match the OpenMP concept with its primary purpose.
>> [!example] Group A
>> a) schedule()
>> b) reduction()
>> c) atomic
>> d) nowait
>
>> [!example] Group B
>> n) Used to avoid race conditions on simple memory updates using hardware.
>> o) Controls the distribution of loop iterations across threads.
>> p) Safely combines private thread values into a global variable.
>> q) Allows threads to skip the implicit barrier at the end of a construct.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)
>> d) -> q)

> [!question] Match the synchronization directive with its behavior.
>> [!example] Group A
>> a) critical
>> b) master
>> c) single
>> d) barrier
>
>> [!example] Group B
>> n) Executed by only one thread, but threads wait at the end due to an implicit barrier.
>> o) Threads must wait until all team members reach this point.
>> p) Ensures only one thread at a time executes the enclosed block.
>> q) Executed by ID 0 only, without an implicit barrier.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> q)
>> c) -> n)
>> d) -> o)

> [!question] Match the variable attribute with its definition.
>> [!example] Group A
>> a) shared
>> b) private
>> c) firstprivate
>
>> [!example] Group B
>> n) Each thread gets a unique local copy initialized with the master's value.
>> o) All threads access the same memory location.
>> p) Each thread has its own local copy without initial values from the master.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the scheduling policy with its description.
>> [!example] Group A
>> a) static
>> b) dynamic
>> c) guided
>
>> [!example] Group B
>> n) Distributes iterations based on a runtime task queue.
>> o) Divides iterations at compile-time in round-robin fashion.
>> p) Starts with large chunks that get smaller to reduce overhead.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the compiler flag with the corresponding vendor.
>> [!example] Group A
>> a) -fopenmp
>> b) -qopenmp
>
>> [!example] Group B
>> n) Intel (OneAPI)
>> o) GNU (GCC)
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)

> [!question] Match the OpenMP term with its definition.
>> [!example] Group A
>> a) Fork
>> b) Join
>> c) Task
>
>> [!example] Group B
>> n) Unit of work generated for asynchronous execution.
>> o) Master thread creates a team of threads.
>> p) Threads synchronize and terminate, leaving only the master.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the troubleshooting strategy with the problem it addresses.
>> [!example] Group A
>> a) Padding
>> b) Reduction
>> c) Taskwait
>
>> [!example] Group B
>> n) Fixes race conditions on sum accumulators.
>> o) Prevents False Sharing by separating variables in memory.
>> p) Ensures direct child tasks are finished before proceeding.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the nesting control term with its definition.
>> [!example] Group A
>> a) OMP_NESTED
>> b) OMP_MAX_ACTIVE_LEVELS
>> c) omp_get_level()
>
>> [!example] Group B
>> n) Returns the current nesting depth.
>> o) Environment variable to enable or disable nested teams.
>> p) Limits the depth of nested parallel regions.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the performance observation with the likely cause.
>> [!example] Group A
>> a) High Barrier Wait Time
>> b) False Sharing
>> c) Serial Code
>
>> [!example] Group B
>> n) Threads fighting for cache line ownership.
>> o) Load imbalance where fast threads wait for slow ones.
>> p) Missing the -fopenmp compiler flag.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the "Thread Analogy" term with its real-world equivalent.
>> [!example] Group A
>> a) Apartment
>> b) Kitchen Table
>> c) Bedroom
>
>> [!example] Group B
>> n) Private Data/Stack.
>> o) Process (Memory Space).
>> p) Communication mechanism for shared variables.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)