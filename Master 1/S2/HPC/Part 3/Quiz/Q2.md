---
sources:
  - "[[High Performance Computing - OpenMP Advanced Concepts]]"
---

> [!question] The `#pragma omp parallel for` directive combines the fork and the work-sharing of loop iterations into a single directive.
>> [!success]- Answer
>> True

> [!question] OpenMP tasks are always executed immediately by the thread that created them.
>> [!success]- Answer
>> False

> [!question] A "Hot Team" is an OpenMP feature that puts thread teams to sleep instead of destroying them when a parallel region ends, allowing for faster subsequent forks.
>> [!success]- Answer
>> True

> [!question] The `omp_get_ancestor_thread_num()` function is used to identify the ID of the thread at a specific level in the nesting hierarchy.
>> [!success]- Answer
>> True

> [!question] The `#pragma omp single nowait` directive forces all threads to wait at an implicit barrier before continuing.
>> [!success]- Answer
>> False

> [!question] In OpenMP, a loop must be in "Canonical Form" to be parallelized by a `for` directive.
>> [!success]- Answer
>> True

> [!question] `omp_set_num_threads()` can only be called from inside a parallel region.
>> [!success]- Answer
>> False

> [!question] False Sharing is only an issue when multiple threads read from the same memory location simultaneously.
>> [!success]- Answer
>> False

> [!question] A `private` variable in OpenMP is initialized with the value of the corresponding variable in the master thread by default.
>> [!success]- Answer
>> False

> [!question] The `omp_get_num_threads()` function returns 1 if called outside of any parallel region.
>> [!success]- Answer
>> True

> [!question] Which clause would you use to ensure a variable is private but inherits its value from the master thread?
> a) private()
> b) shared()
> c) firstprivate()
> d) lastprivate()
>> [!success]- Answer
>> c) firstprivate()

> [!question] What is the default scope of a variable declared outside a parallel region in OpenMP?
> a) Private
> b) Shared
> c) Firstprivate
> d) Atomic
>> [!success]- Answer
>> b) Shared

> [!question] When using `schedule(static, chunk)`, how is the work assigned to threads?
> a) At runtime based on availability.
> b) At compile-time in a round-robin fashion.
> c) Based on the number of available CPU sockets.
> d) Randomly.
>> [!success]- Answer
>> b) At compile-time in a round-robin fashion.

> [!question] What does the `OMP_PROC_BIND` environment variable control?
> a) The number of threads.
> b) Thread placement or affinity to specific hardware/NUMA nodes.
> c) The level of nested parallelism.
> d) The compiler flags.
>> [!success]- Answer
>> b) Thread placement or affinity to specific hardware/NUMA nodes.

> [!question] Which of these is a symptom of load imbalance?
> a) High CPU usage across all cores.
> b) Some threads finish early and wait at the barrier while one is still working.
> c) The program crashes with a segmentation fault.
> d) Every thread finishes at exactly the same time.
>> [!success]- Answer
>> b) Some threads finish early and wait at the barrier while one is still working.

> [!question] What is the "Single Creator Pattern"?
> a) Using a single thread to spawn all tasks to avoid duplication.
> b) Creating one thread for every single iteration of a loop.
> c) Forcing the master thread to do all the work.
> d) Using one processor core for the entire program.
>> [!success]- Answer
> a) Using a single thread to spawn all tasks to avoid duplication.

> [!question] Why is `atomic` preferred over `critical` for a counter increment?
> a) Because `critical` is hardware-based and `atomic` is software-based.
> b) Because `atomic` leverages low-overhead hardware instructions, while `critical` uses heavy software locks.
> c) Because `critical` can only be used by the master thread.
> d) There is no difference between them.
>> [!success]- Answer
>> b) Because `atomic` leverages low-overhead hardware instructions, while `critical` uses heavy software locks.

> [!question] Which condition is required for the `taskwait` directive?
> a) All threads must be at the same barrier.
> b) It suspends the current task until its direct child tasks are complete.
> c) It terminates the program.
> d) It only works on nested parallel regions.
>> [!success]- Answer
>> b) It suspends the current task until its direct child tasks are complete.

> [!question] What happens if `OMP_NESTED` is set to `false`?
> a) Nested parallel regions execute as a team of 1 thread.
> b) The program will fail to compile.
> c) Nested regions are not allowed and will throw an error.
> d) Nested regions are executed in parallel using all available cores.
>> [!success]- Answer
>> a) Nested parallel regions execute as a team of 1 thread.

> [!question] What is the primary purpose of adding "padding" to a struct?
> a) To save memory.
> b) To ensure variables land on different cache lines to avoid False Sharing.
> c) To make the code easier to read.
> d) To speed up the compiler.
>> [!success]- Answer
>> b) To ensure variables land on different cache lines to avoid False Sharing.

> [!question] Match the OpenMP clause with its specific functionality.
>> [!example] Group A
>> a) collapse(N)
>> b) firstprivate(var)
>> c) schedule(dynamic, chunk)
>> d) nowait
>
>> [!example] Group B
>> n) Allows threads to bypass the implicit barrier.
>> o) Flattens N nested loops into one.
>> p) Initializes a private copy with the master's variable value.
>> q) Assigns loop work chunks at runtime based on demand.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> q)
>> d) -> n)

> [!question] Match the hardware-related term with its description.
>> [!example] Group A
>> a) Cache Line
>> b) NUMA
>> c) Hot Team
>> d) Oversubscription
>
>> [!example] Group B
>> n) Architecture where memory access latency depends on memory location relative to the processor.
>> o) Running more threads than available physical hardware cores.
>> p) The standard 64-byte block of memory loaded by CPUs.
>> q) Runtimes keeping thread teams in "sleep" mode for quick reuse.
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> q)
>> d) -> o)

> [!question] Match the error type with its definition.
>> [!example] Group A
>> a) Race Condition
>> b) False Sharing
>> c) Load Imbalance
>
>> [!example] Group B
>> n) Performance loss caused by threads competing for cache lines.
>> o) Non-deterministic results due to timing and unsynchronized writes.
>> p) Inefficiency caused by uneven distribution of work among threads.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the function with its role in nested parallelism.
>> [!example] Group A
>> a) omp_get_level()
>> b) omp_set_max_active_levels()
>> c) omp_get_ancestor_thread_num()
>
>> [!example] Group B
>> n) Returns the number of the thread that spawned the current one.
>> o) Returns the current nesting depth.
>> p) Controls the maximum nesting depth allowed.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the "Why?" reason for each OpenMP directive.
>> [!example] Group A
>> a) #pragma omp task
>> b) #pragma omp barrier
>> c) #pragma omp atomic
>> d) #pragma omp master
>
>> [!example] Group B
>> n) Only allow one thread to perform an operation without an implicit barrier.
>> o) Handle dynamic workloads (irregular structures).
>> p) Use hardware-level locks for simple arithmetic.
>> q) Synchronize all threads at a specific point.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> q)
>> c) -> p)
>> d) -> n)

> [!question] Match the scheduling policy with the ideal use case.
>> [!example] Group A
>> a) static
>> b) dynamic
>> c) guided
>
>> [!example] Group B
>> n) Unpredictable work where load balancing is critical.
>> o) Predictable, equal work with low overhead.
>> p) Situations needing to reduce dynamic overhead while maintaining balance.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the directive to its implicit barrier behavior.
>> [!example] Group A
>> a) parallel
>> b) for (work-sharing)
>> c) single
>> d) master
>
>> [!example] Group B
>> n) Has an implicit barrier.
>> o) Does not have an implicit barrier.
>
>> [!success]- Answer
>> a) -> n)
>> b) -> n)
>> c) -> n)
>> d) -> o)

> [!question] Match the synchronization tool with the problem it prevents.
>> [!example] Group A
>> a) Reduction
>> b) Critical
>> c) Taskwait
>
>> [!example] Group B
>> n) Incorrect data from unfinished child tasks in recursive calls.
>> o) Accumulation errors in parallel loops.
>> p) Race conditions in complex, non-atomic code blocks.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the memory state description with the concept.
>> [!example] Group A
>> a) Shared
>> b) Private
>> c) Isolated
>
>> [!example] Group B
>> n) Data is restricted to a single process (MPI).
>> o) Data is accessed by the whole team in the same address space.
>> p) Data is stored on the stack of a specific thread.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the action with the effect on OpenMP overhead.
>> [!example] Group A
>> a) Creating many nested teams
>> b) Using atomic instead of critical
>> c) Using static instead of dynamic scheduling
>
>> [!example] Group B
>> n) Reduces overhead.
>> o) Increases context-switching overhead.
>> p) Reduces synchronization overhead.
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)