---
sources:
  - "[[07.1. ViewSets Concepts and Action Mappings]]"
  - "[[07.2. ViewSets Structural Class Hierarchy]]"
  - "[[07.3. ModelViewSet and CRUD Automation Engine]]"
  - "[[07.4. ViewSets Hook Methods and Runtime Overrides]]"
  - "[[07.5. Custom Actions and Routes using action]]"
  - "[[07.6. DRF Routing Automations SimpleRouter]]"
  - "[[07.7. DRF Routing Automations DefaultRouter]]"
---
> [!question] A `GenericViewSet` inherits from `GenericAPIView` and includes default action methods like `list()` and `create()`.
>> [!success]- Answer
>> False

> [!question] The `@action` decorator's `detail` parameter determines whether the action applies to a single record or the entire collection.
>> [!success]- Answer
>> True

> [!question] If a ViewSet does not define a `queryset` attribute but does override `get_queryset()`, you must specify `basename` when registering with a router.
>> [!success]- Answer
>> True

> [!question] The `DefaultRouter` can only register one ViewSet per project.
>> [!success]- Answer
>> False

> [!question] In a ViewSet, a collection action (`detail=False`) cannot access `self.get_object()` or the `pk` parameter.
>> [!success]- Answer
>> True

> [!question] The `ReadOnlyModelViewSet` is suitable for lookup tables where users should not create, update, or delete data.
>> [!success]- Answer
>> True

> [!question] `ModelViewSet`'s `perform_update()` hook is called right after the serializer validates data but before saving.
>> [!success]- Answer
>> True

> [!question] QuerySets defined at the class level in a ViewSet execute immediately when the server starts.
>> [!success]- Answer
>> False

> [!question] Custom actions that accept POST requests must specify `methods=['POST']` in the `@action` decorator.
>> [!success]- Answer
>> True

> [!question] The `PATCH` HTTP method maps to the `partial_update` action in a ViewSet.
>> [!success]- Answer
>> True

> [!question] Which ViewSet class should you use when you want complete control and don't need database query helpers?
> a) ModelViewSet
> b) GenericViewSet
> c) ViewSet
> d) ReadOnlyModelViewSet
>> [!success]- Answer
>> c) ViewSet

> [!question] What is the purpose of `get_serializer_class()` in a ViewSet?
> a) To define the model for the ViewSet
> b) To use different serializers for different actions
> c) To set the permission class
> d) To define the queryset
>> [!success]- Answer
>> b) To use different serializers for different actions

> [!question] How does a Detail custom action differ from a Collection custom action?
> a) Detail actions cannot use POST
> b) Detail actions include a primary key in the URL
> c) Detail actions only work with GET
> d) There is no difference
>> [!success]- Answer
>> b) Detail actions include a primary key in the URL

> [!question] What URL patterns does `SimpleRouter` generate for a ViewSet registered with prefix `patients`?
> a) /patients/ and /patients/{pk}/
> b) /patients-list/ and /patients-detail/
> c) /api/patients/ only
> d) /patients/ only
>> [!success]- Answer
>> a) /patients/ and /patients/{pk}/

> [!question] Which ViewSet class inherits from `GenericAPIView` but does NOT include default action methods?
> a) ModelViewSet
> b) ReadOnlyModelViewSet
> c) GenericViewSet
> d) ViewSet
>> [!success]- Answer
>> c) GenericViewSet

> [!question] In the `@action` decorator, what does `detail=True` generate in the URL?
> a) No change to the URL
> b) The URL includes `{pk}` before the action name
> c) The URL is prefixed with `detail/`
> d) The URL ends with `/list/`
>> [!success]- Answer
>> b) The URL includes `{pk}` before the action name

> [!question] What is the correct router method to register a ViewSet?
> a) router.add()
> b) router.register()
> c) router.include()
> d) router.viewset()
>> [!success]- Answer
>> b) router.register()

> [!question] What does the `router.urls` property return?
> a) A single URL pattern
> b) A list of generated URL patterns
> c) The router configuration dictionary
> d) A URL pattern string
>> [!success]- Answer
>> b) A list of generated URL patterns

> [!question] How are custom action URL patterns generated?
> a) They replace the standard actions
> b) They are appended to the standard list and detail patterns
> c) They are not generated by the router
> d) They require manual URL configuration
>> [!success]- Answer
>> b) They are appended to the standard list and detail patterns

> [!question] What happens if you call `self.get_object()` inside a collection custom action?
> a) It works normally
> b) It raises an error because there is no pk in the URL
> c) It returns the first object
> d) It returns a random object
>> [!success]- Answer
>> b) It raises an error because there is no pk in the URL

> [!question] Match the ViewSet type with its use case.
>> [!example] Group A
>> a) ViewSet
>> b) GenericViewSet + Mixins
>> c) ReadOnlyModelViewSet
>
>> [!example] Group B
>> n) Read-only resources like lookup tables
>> o) Support only specific actions selectively
>> p) Highly custom APIs not tied to a model
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the hook method with its override purpose.
>> [!example] Group A
>> a) get_queryset
>> b) get_serializer_class
>> c) perform_create
>
>> [!example] Group B
>> n) Assign logged-in user as creator
>> o) Use lightweight serializer for list views
>> p) Filter by current user's permissions
>
>> [!success]- Answer
>> a) -> p)
>> b) -> o)
>> c) -> n)

> [!question] Match the action type with its `@action` parameter.
>> [!example] Group A
>> a) Collection action
>> b) Detail action
>> c) POST method action
>
>> [!example] Group B
>> n) detail=True
>> o) methods=['POST']
>> p) detail=False
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the URL component with its pattern after router registration.
>> [!example] Group A
>> a) /patients/
>> b) /patients/{pk}/
>> c) /patients/active/
>
>> [!example] Group B
>> n) Collection custom action
>> o) List standard action
>> p) Detail standard action
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the ViewSet mixin with the action it provides.
>> [!example] Group A
>> a) RetrieveModelMixin
>> b) UpdateModelMixin
>> c) ListModelMixin
>
>> [!example] Group B
>> n) update() and partial_update()
>> o) list()
>> p) retrieve()
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)

> [!question] Match the router output with its generated route name.
>> [!example] Group A
>> a) GET /patients/
>> b) POST /patients/
>> c) GET /patients/1/
>
>> [!example] Group B
>> n) patient-list (list)
>> o) patient-detail (retrieve)
>> p) patient-list (create)
>
>> [!success]- Answer
>> a) -> n)
>> b) -> p)
>> c) -> o)

> [!question] Match the `DefaultRouter` feature with its purpose.
>> [!example] Group A
>> a) API Root View
>> b) Format Suffix
>> c) Standard URL patterns
>
>> [!example] Group B
>> n) /api/patients.json
>> o) List of all registered endpoints
>> p) list, create, retrieve, update, destroy
>
>> [!success]- Answer
>> a) -> o)
>> b) -> n)
>> c) -> p)

> [!question] Match the ViewSet attribute with its configuration.
>> [!example] Group A
>> a) queryset
>> b) serializer_class
>> c) permission_classes
>
>> [!example] Group B
>> n) [IsEditorOrAdminOnly]
>> o) Patient.objects.all()
>> p) PatientModelSerializer
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the model action with its ViewSet method.
>> [!example] Group A
>> a) Full update
>> b) Partial update
>> c) Single record retrieval
>
>> [!example] Group B
>> n) retrieve()
>> o) update()
>> p) partial_update()
>
>> [!success]- Answer
>> a) -> o)
>> b) -> p)
>> c) -> n)

> [!question] Match the router concept with its code.
>> [!example] Group A
>> a) Router instantiation
>> b) ViewSet registration
>> c) URL pattern inclusion
>
>> [!example] Group B
>> n) router.register(r'patients', PatientViewSet)
>> o) urlpatterns = router.urls
>> p) router = SimpleRouter()
>
>> [!success]- Answer
>> a) -> p)
>> b) -> n)
>> c) -> o)